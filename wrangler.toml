name = "school"             # Your Worker name
main = "api.js"              # or "api.js"
compatibility_date = "2024-10-01"

[[d1_databases]]
binding = "DB"
database_name = "gstar-db"
database_id = "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"   # Replace with your real D1 ID

[vars]
# Never commit real secrets; set them in the dashboard instead.
# JWT_SECRET = "set-in-dashboard"
# ADMIN_EMAIL = "set-in-dashboard"
# ADMIN_PASSWORD = "set-in-dashboard"
